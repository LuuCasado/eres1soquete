import { createUseStyles } from 'react-jss'

export default createUseStyles({
  contentStyle: {
    marginBottom: "2rem",
    backgroundColor: "#b99cf0",
    maxWidth: "100%",
    height: "15rem",
    textAlign: "center"
  }
})