import { createUseStyles } from 'react-jss'

export default createUseStyles({
  dropdownClickable: {
    cursor: "pointer",
    display: "flex",
  },

  settingsIcon: {
    width: "1.5rem",
    fill: "white",
    marginLeft: "1rem",
    cursor: "pointer"
  }

})